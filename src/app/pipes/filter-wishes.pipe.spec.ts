import { FilterWishesPipe } from './filter-wishes.pipe';

describe('FilterWishesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWishesPipe();
    expect(pipe).toBeTruthy();
  });
});
